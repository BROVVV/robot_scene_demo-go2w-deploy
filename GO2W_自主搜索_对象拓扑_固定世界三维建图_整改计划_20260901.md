# GO2-W 自主搜索、对象拓扑与固定世界三维建图整改计划

**编写日期：** 2026-09-01（Asia/Shanghai）  
**项目主机：** `/home/mxt/robotscene`  
**机器狗运行目录：** `/home/unitree/robotscene`  
**真机会话：** `outputs/live_runs/search_20260901_211756_3460e15a`  
**目标：** 一次解决转向角度不一致、对象拓扑显示错误、搜索提前结束和三维地图重影四个问题。

---

## 1. 给执行 AI 的最终目标

完成后必须同时满足：

1. 规划显示右转或左转 60° 时，机器人实际累计转角就是 60°左右，不再只执行一个 30°动作；
2. WebUI 的“语义拓扑”只显示物体节点以及物体与物体之间的关系，不显示 `P1`、`F01`、`F02`、`OBSERVED_FROM`、`FRONTIER_TO`；
3. 搜索不会在三四步后因为程序异常或暂时没有候选而伪报 `SEARCH_EXHAUSTED`；
4. 三维地图使用固定世界坐标，机器人原地转向时场景不跟着转、不复制，机器人行走时地图在原地图上连续扩展；
5. 所有改动部署到机器狗，并完成离线测试、ROS 数据链检查、遥控器建图验收和 WebUI 搜索验收。

本计划要求修现有链路，不引入新的大型框架，不重写搜索系统，不通过前端视觉补偿掩盖错误点云。

---

## 2. 已确认的运行证据

### 2.1 本次搜索不是预算耗尽

会话的预算为：

```text
max_search_seconds = 600
max_planning_cycles = 100
max_motion_steps = 50
max_consecutive_no_information_cycles = 20
```

实际结束时：

```text
duration_s = 212.534
planning_cycles = 4
motion_steps = 3
observations = 4
finish_reason = SEARCH_EXHAUSTED
```

结束前的首个决定性异常是：

```text
candidate_generator_error:
TypeError: warn() takes 2 positional arguments but 4 were given
```

随后 `AutonomousExplorer` 把异常转换成空候选，再立即输出：

```text
search_exhausted: no exploration candidates
```

因此这是软件异常被误分类，不是真的搜完了。

### 2.2 60°只执行30°已经由事件日志证实

三次动作如下：

| 规划目标 | 实际 primitive | 里程计实测 |
|---|---|---:|
| `+30°` | `l30` | `+28.97°` |
| `-60°` | `r30` | `-28.52°` |
| `+60°` | `l30` | `+28.88°` |

`app/navigation/local_scan.py` 会生成 `±30°/±60°`；但
`app/navigation/go2w_experimental_backend.py::_execute_turn()` 把任意请求静默限幅到
`max_turn_deg_per_action=30`，且只执行一次。规划层和执行层的目标契约不一致。

### 2.3 对象专用拓扑已经存在，前端却取错对象

后端已有：

```text
semantic_graph.object_topology
schema_version = semantic_object_topology_v1
nodes = OBJECT only
edges = OBJECT ↔ OBJECT only
```

但 `app/manual_web_demo/static/search_map.js` 当前逻辑优先使用整个
`semantic_navigation_graph_v1`：

```javascript
var graph = this.spatial.semantic_graph || null;
var topology = graph || null;
```

只有整个图为空时才退回 `graph.object_topology`。所以 `PLACE`、`FRONTIER`、
`OBSERVED_FROM` 和 `FRONTIER_TO` 被画到了“语义拓扑”页。

### 2.4 三维重影来自上游 LIO 动态发散

本次只执行三次原地转向，融合轮式里程计结束位姿约为：

```text
x = -0.116 m
y = -0.264 m
yaw = -84.5°
```

LIO 的 `/go2w/slam/odom_base` 却最终变为：

```text
x = 3.445 m
y = 9.101 m
yaw = +67.9°
```

桥接日志还记录了多个离散错误位置：

```text
(-0.298, 0.028) → (-1.078, -0.489)
→ (-3.255, 0.716) → (1.782, 1.084)
→ (3.445, 9.101)
```

机器人原地转向时 LIO 却产生数米平移，说明动态点云注册的时间/IMU轴向/外参或 LIO
估计存在错误。Web 点云桥没有再次旋转点云，它只是永久累积已经被错误标成
`pslam_odom` 世界坐标的 `aligned_scan`。

另外当前 Web 桥还存在两个放大问题：

1. `/go2w/slam/map_3d` 使用 `pslam_map`，而 Web 桥目标帧为 `pslam_odom`，所以优化后的
   SLAM 全局地图因 `map_frame_mismatch` 被丢弃；界面实际长期显示
   `source=aligned_scan_accumulated`；
2. 当前漂移门只检查相邻单帧变化。LIO 在运动期间逐步漂移，停稳后错误位姿变得“稳定”，
   连续三帧便恢复成 `HEALTHY`，于是错误扫描仍进入永久地图。

当前快照已经显示不合理结果：

```text
mapping_health = HEALTHY          # 误判
accumulated_voxels = 40000
bounds ≈ 22.1 m × 26.7 m
```

---

## 3. 总体修改顺序

必须按以下顺序执行，上一阶段未通过不得用后续显示层补偿：

1. 固化本次失败会话为回归样本；
2. 修复 60°动作契约；
3. 修复候选生成异常和错误终止语义；
4. 把 WebUI 拓扑切换为已有的对象专用投影；
5. 采集动态 LIO 对照数据并修正时间、轴向和外参；
6. 改正三维地图的全局帧和永久地图数据源；
7. 改正地图漂移门和 session reset；
8. 完成离线、静止、遥控转向、遥控行走和自主搜索分级验收；
9. 部署、重启 WebUI，并保留最终日志和报告。

---

## 4. 阶段一：固化失败回归样本

### 4.1 保存证据

从机器狗复制以下文件到主机新目录，禁止修改原日志：

```text
outputs/live_runs/search_20260901_211756_3460e15a/events.jsonl
outputs/autonomous_search/logs/search_worker.log
outputs/autonomous_search/logs/slam_web_bridge.log
outputs/autonomous_search/runtime/slam_map_3d.json
```

建议目录：

```text
outputs/regressions/search_20260901_211756_3460e15a/
```

### 4.2 新增回归测试

新增：

```text
tests/test_regression_search_20260901_211756.py
```

至少断言：

- `-60°` 目标不能只生成一个 `r30` 后宣告成功；
- `+60°` 目标不能只生成一个 `l30` 后宣告成功；
- logger 调用不会抛 `TypeError`；
- candidate generator 抛异常时结果不能是 `SEARCH_EXHAUSTED`；
- object topology 投影不含 Place/Frontier；
- 原地旋转期间 LIO 出现数米平移时地图健康状态不得为 `HEALTHY`。

---

## 5. 阶段二：修复 60°只执行30°

### 5.1 修改原则

保留单个物理 primitive 最大 30°的现有安全限制。一个逻辑 60°目标拆成两个 30°
primitive，并依据实际里程计闭环完成总角度；不要把安全上限直接改成 60°。

### 5.2 修改文件

```text
app/navigation/go2w_experimental_backend.py
app/navigation/models.py
app/live_robot/autonomous_explorer.py
scripts/go2w/run_semantic_exploration.py
app/manual_web_demo/static/search_ui.js
tests/test_go2w_experimental_backend.py
tests/test_local_scan.py
```

### 5.3 执行逻辑

在 `_execute_turn()` 中：

1. 保存逻辑请求 `requested_total_deg`；
2. 按 30°拆分，例如 `+60 → [l30, l30]`，`-60 → [r30, r30]`；
3. 每段执行前后读取 canonical `/go2w/odom/fused`；
4. 每段必须通过现有停止和角度验证；
5. 用累计实测 yaw 判断逻辑目标是否完成；
6. 第一段失败时禁止执行第二段；
7. 两段都完成后才返回逻辑 goal `SUCCEEDED`；
8. 若第一段实际转过头，第二段使用剩余角度，不能机械地再转满30°；
9. 最终误差超阈值时返回 `FAILED/ROTATION_NOT_CONFIRMED`，不能只根据 RPC success 成功。

建议事件字段：

```json
{
  "requested_total_deg": -60.0,
  "segment_count": 2,
  "segment_index": 2,
  "segment_step": "r30",
  "observed_total_deg": -57.4,
  "remaining_deg": -2.6
}
```

WebUI 显示“右转60°（2/2，已完成57.4°）”，不要把逻辑目标和单个 primitive 混为一条。

### 5.4 验收标准

- `±30°`：仍只执行一个 primitive；
- `±60°`：正常情况下执行两个 primitive；
- 实测累计角度与目标同号；
- 60°目标最终误差绝对值不大于 8°；
- 任一段失败后机器人停止，逻辑目标不成功；
- event 中同时保留逻辑请求、分段请求和累计实测角度。

---

## 6. 阶段三：修复搜索三四步后提前结束

### 6.1 修复确定的 Foxy logger 错误

文件：

```text
scripts/go2w/run_semantic_exploration.py
```

当前 ROS 2 Foxy logger 的 `warn()` 不接受 Python logging 风格的多个格式参数。把
2090 行附近改为单字符串，并统一使用 `warning()`：

```python
node.get_logger().warning(
    f"metric map not fresh; falling back "
    f"(provider={args.spatial_provider}, frontier count={len(frontiers)})"
)
```

同文件内其余 `warn()` 调用也检查一次，但只修改确实不兼容的调用，不做全项目机械重写。

### 6.2 修正异常分类

文件：

```text
app/live_robot/autonomous_explorer.py
app/live_robot/explorer_search_adapter.py
app/live_robot/search_state_store.py
```

当前逻辑是：

```text
candidate generator exception → candidates=[] → SEARCH_EXHAUSTED
```

修改为：

```text
candidate generator exception
→ 记录 PLANNING_ERROR/candidate_generator
→ 停稳
→ 允许一次新观测后的重新规划
→ 同一异常再次发生则 FAILED（不是 SEARCH_EXHAUSTED）
```

`SEARCH_EXHAUSTED` 只能表示真实搜索空间穷尽，不能表示 Python 异常、地图暂时不新鲜或
ROS 数据暂时缺失。

### 6.3 修正“空候选”的语义

空候选必须分三类：

| 情况 | 正确处理 |
|---|---|
| metric map stale/unavailable | `WAITING_FOR_MAP`，停稳并等待新地图后重规划 |
| 当前 Place 本地扫描配额耗尽，但有可达 frontier | 选择长期 frontier，继续移动 |
| 地图健康、全方向已扫描、所有可达 frontier 均已访问或失败 | 才允许 `SEARCH_EXHAUSTED` |

实现时复用已有 `frontier_extractor`、`spatial_reasoner`、`TopologyRoutePlanner`，不要增加第二套
候选系统。

### 6.4 local scan 配额规则

本次 `P1` 执行三次旋转后配额耗尽是正常的，错误在于长期目标分支随 logger 异常一起失败。
修复后要求：

- 配额只限制同一 Place 的原地扫描，不代表全局搜索结束；
- 真平移到新 Place 后有新的局部扫描配额；
- 配额耗尽时必须进入 frontier/route 分支；
- 没有健康地图时不允许盲当前进，应等待地图恢复并向 WebUI明确显示原因。

### 6.5 验收标准

- 重放本次会话不再出现 `warn() takes 2 positional arguments`；
- candidate generator 人工注入异常时结果为 `PLANNING_ERROR/FAILED`，不是
  `SEARCH_EXHAUSTED`；
- 有可达 frontier 时，三次 local scan 后第四次规划选择 frontier；
- 目标未找到且仍有可达区域时，搜索可持续超过 10 个 motion step；
- 只有满足真实穷尽条件时才输出 `SEARCH_EXHAUSTED`，事件里列出已扫描方向数、可达
  frontier 数、已访问数和不可达数。

---

## 7. 阶段四：WebUI只显示物体拓扑

### 7.1 修改文件

```text
app/manual_web_demo/static/search_map.js
app/manual_web_demo/static/search_ui.js
tests/test_go2w_live_ui_status.py
tests/test_search_state_store_spatial.py
```

### 7.2 主修复

`SearchMapRenderer.render()` 在语义拓扑模式必须直接使用：

```javascript
var graph = this.spatial.semantic_graph || null;
var topology = graph && graph.object_topology
  ? graph.object_topology
  : null;
```

禁止优先把整个 `semantic_navigation_graph_v1` 传给 `renderObjectTopology()`。

### 7.3 双层数据约束

后端 `object_topology_snapshot()` 已经按对象过滤，保留该实现。前端再做最小契约检查：

- 节点只接受 `node_type === "OBJECT"`；
- 边的 `from/to` 必须都存在于当前对象节点集合；
- 不接受 `OBSERVED_FROM`、`FRONTIER_TO`、`MOVED_TO`；
- 不通过 ID 前缀猜测节点类型，使用 `node_type`；
- 没有对象关系时可以显示孤立对象节点，但不能退回导航图。

导航内部仍保留 Place 和 Frontier，规划器仍可使用它们。这里只改变“语义拓扑”页面的
投影，不能删除内部导航图数据。

### 7.4 验收标准

浏览器中：

- 节点只出现 `obj_001`、`obj_002` 等对象；
- 不出现 `P1/P2/F01/F02`；
- 不出现 `OBSERVED_FROM/FRONTIER_TO/MOVED_TO`；
- 边只连接两个对象；
- 节点标题同时显示稳定对象 ID 和中文标签；
- 刷新页面和新 session 后仍满足以上规则。

---

## 8. 阶段五：先修 LIO 动态位姿，再谈三维地图显示

### 8.1 当前不能接受的状态

当前使用：

```text
LiDAR: Hesai PandarXT-16
IMU: /utlidar/imu
IMU timestamp: receive-sensor offset aligner
Pandar↔base extrinsic: candidate_unconfirmed
LIO: plain_slam loose coupling
```

静止通过不代表动态通过。当前候选外参和接收时间对齐在原地转向中已经产生数米假平移，
禁止把它标记为正式标定通过。

### 8.2 采集最小动态标定数据

由用户用遥控器操控，执行 AI 只负责记录和监视，不下发自主运动：

1. 静止 60 秒；
2. 左转约30°，停5秒；
3. 再左转约30°，停5秒；
4. 右转约30°，停5秒；
5. 原地一周，低速完成；
6. 直行约1米，停5秒；
7. 原路退回或掉头返回起点。

必须同步记录：

```text
/hesai/pandarxt16/points_raw
/go2w/slam/pandar_points
/utlidar/imu
/go2w/slam/imu
/go2w/slam/imu_pose_raw
/go2w/slam/odom_base
/go2w/odom/fused
/go2w/slam/aligned_scan
/go2w/slam/map_3d
/tf
/tf_static
```

每个动作由用户口头或按键标记开始/停止时间，保存为一个 bag 和一份动作时间表。

### 8.3 先验证时间和轴向

新增或扩展只读分析脚本：

```text
scripts/go2w/analyze_lio_motion_alignment.py
```

脚本输出：

- `/utlidar/imu` 与 Pandar cloud 时间差的 median、p95、max；
- IMU `angular_velocity.{x,y,z}` 与 `/go2w/odom/fused` yaw rate 的相关系数；
- 最佳时间偏移及其置信区间；
- 左/右转时角速度符号是否与 fused odom 一致；
- 每个转向段的 wheel Δxy/Δyaw 与 pslam Δxy/Δyaw；
- 每段点云 scan-to-scan ICP 的旋转和平移残差。

判定顺序：

1. 若 IMU yaw 轴或符号错误，先修轴向变换；
2. 若角速度峰值时间错位，先修固定时间偏移；
3. 时间和轴向正确后，再求 Pandar↔IMU外参；
4. 最后才调整 LIO 匹配参数。

不要通过增大 correspondence distance 或降低健康阈值掩盖时间/外参错误。

### 8.4 修正 timestamp aligner

文件：

```text
ros2_ws/src/go2w_plain_slam_bridge/scripts/imu_timestamp_aligner.py
ros2_ws/src/go2w_plain_slam_bridge/scripts/imu_fallback_adapter.py
```

要求：

- 同一个 mapping session 使用经启动窗口估计并锁定的 sensor→ROS 时钟偏移；
- 运动期间不根据 callback 到达抖动持续拖动时间轴；
- 原始 sensor stamp、修正 stamp、offset 和模式进入诊断；
- 时间回退、重复或大跳变时停止给 LIO喂数据并要求重启 session；
- 不用“当前 callback 时间”直接覆盖传感器采样时间；
- Pandar point 内相对时间字段保持原值，deskew 使用同一时间语义。

### 8.5 修正 IMU轴向和外参

相关文件：

```text
configs/go2w/hesai_pandarxt16_extrinsics.yaml
scripts/go2w/generate_plain_slam_pandar_config.py
runtime/go2w/plain_slam/generated_lio_3d_params.yaml
```

执行要求：

- 根据动态 bag 求得 IMU→Pandar 的旋转和平移；
- 左右转都必须验证，不能只拟合单一方向；
- 直行段用于排除把转向误拟合成平移；
- 生成文件必须记录标定来源、bag路径、残差和配置哈希；
- `candidate_unconfirmed` 只有通过本计划动态门槛后才升级；
- 不手改 `runtime` 生成文件，修改源 YAML 后重新生成。

### 8.6 LIO 动态验收门槛

每个30°原地转向段：

```text
fused Δxy ≤ 0.10 m
pslam Δxy ≤ 0.20 m
pslam 与 fused yaw 同号
|pslam Δyaw - fused Δyaw| ≤ 5°
```

360°闭环：

```text
pslam position closure error ≤ 0.30 m
pslam yaw closure error ≤ 10°
同一墙面/桌面点云不得出现多份明显旋转副本
```

1米直行：

```text
pslam 与 fused 位移方向一致
位移长度误差 ≤ max(0.20 m, 20%)
结束 yaw 漂移 ≤ 5°
```

任一门槛不通过，不能进入 Web 三维地图最终验收。

---

## 9. 阶段六：改成真正的固定世界三维地图

### 9.1 只保留一个永久地图权威源

修改：

```text
scripts/go2w/plain_slam_web_bridge.py
scripts/go2w/plain_slam_web_cloud_relay.py
app/spatial/pointcloud_web_codec.py
app/manual_web_demo/slam_map_snapshot.py
app/manual_web_demo/static/slam_map_3d.js
scripts/go2w/start_autonomous_search_web.sh
scripts/go2w/start_plain_slam_mapping.sh
tests/test_plain_slam_webui_3d.py
```

永久地图以 `/go2w/slam/map_3d` 的优化历史为权威，不再把每个
`/go2w/slam/aligned_scan` 不可逆地永久塞进同一个 voxel 容器。

建议契约：

```text
canonical map frame = pslam_map
authoritative permanent cloud = /go2w/slam/map_3d
latest scan = 仅实时预览或等待下一个 map keyframe
```

如果 `aligned_scan` 为 `pslam_odom`，而 `map_3d` 为 `pslam_map`：

- 没有时间戳对应的 `T_pslam_map_pslam_odom` 时，不混合两者；
- 有可靠变换时，latest scan 变换一次后只作为预览；
- 新 `map_3d` 到达时整体替换永久地图，使回环优化能纠正历史；
- 不通过修改 `header.frame_id` 假装完成坐标变换。

### 9.2 不再使用“静止后累积几帧”伪装连续建图

当前 Web 桥运动时丢弃全部 scan，停稳后再积累。这只能减少污染，不能形成真正随走随扩展
的地图。修改后：

- LIO/SLAM本身连续处理运动点云；
- Web永久地图跟随 `map_3d` keyframe更新；
- Web可显示 latest scan，但不得写入永久历史；
- UI明确显示“全局地图更新时间”和“当前scan时间”；
- 地图点数达到上限时使用体素降采样，不能按到达顺序删除整片旧区域。

### 9.3 分离“完整地图容量”和“WebUI显示点数”

当前实现的三个限制是：

```text
max_input_points = 8,000 / scan
max_accumulated_points = 40,000 voxels
max_web_points = 20,000 points / snapshot
voxel_size = 0.12 m
```

其中 `20,000` 是 WebUI快照的显示采样上限；更严重的是
`BoundedVoxelCloud` 只保留 `40,000` 个体素，超过后使用
`popitem(last=False)` 按时间删除最早加入的体素。机器人持续进入新区域后，旧房间会逐渐
从地图中消失。这种实现只能支持小范围预览，不能作为大范围三维地图。

修改后必须形成三个互相独立的容量层：

| 层 | 用途 | 容量规则 |
|---|---|---|
| SLAM权威地图 | 保存完整、可回环修正的世界地图 | 读取 `/go2w/slam/map_3d`，不受 Web点数上限影响 |
| Web桥全局缓存 | 对权威地图做体素化、生成快照 | 建议基线30万个全局体素，可配置20万～50万；超限时对全图重新均匀降采样 |
| 浏览器显示 | Three.js实时浏览 | 默认约5万个点，可配置到10万个；从整个地图均匀抽样，不只保留最新区域 |

具体要求：

1. 删除 `OrderedDict` 的“满了就删除最旧体素”策略；
2. 权威 `/go2w/slam/map_3d` 到达后，先在固定世界坐标中进行全局体素降采样；
3. 若全局缓存超过配置上限，对全部空间重新采样，保证地图边界内每个区域仍有代表点；
4. Web snapshot 从完整全局缓存做均匀空间采样，不能按插入顺序截断；
5. `max_web_points` 只控制浏览器传输和绘制负载，绝不能改变SLAM地图内容；
6. 默认将 `max_web_points` 从20,000调整到约50,000，真机测得浏览器性能不足时再降低，
   但降低后仍必须保持全地图空间覆盖；
7. Web传输频率与点数配套调整，避免用JSON以1.5Hz反复发送超大点集拖慢机器狗；地图
   没有新revision时不重复编码同一份点云；
8. UI同时显示“SLAM源点数、全局缓存体素数、Web显示点数和地图范围”，防止把
   `point_count=20000` 误解成完整地图只有两万个点。

建议快照新增字段：

```json
{
  "source_map_points": 486320,
  "global_cached_voxels": 286441,
  "web_display_points": 50000,
  "global_voxel_size_m": 0.12,
  "web_sampling": "global_spatial_uniform",
  "capacity_limited": false
}
```

不要为了追求点数保留无效噪声。大范围能力的判据是旧区域不丢、全局结构完整，而不是
单纯让浏览器渲染几十万重复点。

### 9.4 修正 reset 语义

当前 reset marker 只清 Web voxel，LIO已经发散时下一帧仍是坏世界坐标。新 session reset
必须同时做到：

1. 停止当前地图写入；
2. 清 Web snapshot；
3. 调用 LIO/SLAM reset service；如果上游没有 reset service，则有序重启 mapping launch；
4. 等待 IMU、LiDAR、odom和 map frame重新健康；
5. 新 session ID 到达后才开始显示新地图；
6. 旧 session 的迟到消息按 session/启动时间丢弃。

不需要建立复杂地图版本系统，一个单调 `mapping_session_id` 和启动时间即可。

---

## 10. 阶段七：修正地图健康门，禁止坏位姿停稳后复活

### 10.1 使用完整运动段比较

修改：

```text
app/spatial/spatial_pose_validator.py
scripts/go2w/plain_slam_web_bridge.py
app/manual_web_demo/slam_map_snapshot.py
```

在 fused odom 从静止→运动时记录：

```text
wheel_start_pose
pslam_start_pose
motion_episode_id
```

运动→停稳后比较整个运动段的累计变化，而不是只比较最后相邻两帧：

```text
wheel_total_dx/dy/dyaw
pslam_total_dx/dy/dyaw
```

原地转向时 wheel 平移很小而 pslam 平移超过0.20米，整段 LIO pose epoch 判为
`DEGRADED_LIO_DRIFT`。

### 10.2 恢复规则

以下行为必须删除：

```text
错误位姿停稳 → 连续3个变化很小的scan → 自动HEALTHY
```

正确规则：

- 漂移发生后保留 last-good global map；
- 当前 scan 可以在单独调试层显示，但不进入永久地图；
- 只有 LIO reset/relocalization 后，新 session 从一致起点建立，并连续通过静止和一个小动态
  检查，才恢复 `HEALTHY`；
- WebUI显示明确中文原因，例如“LIO在原地转向中产生2.9米假平移，地图已冻结”；
- readiness在地图不健康时禁止自主平移搜索，但遥控建图诊断仍可只读观察。

### 10.3 必须输出的诊断字段

```json
{
  "mapping_session_id": "...",
  "canonical_frame": "pslam_map",
  "permanent_source": "map_3d",
  "mapping_health": "HEALTHY|DEGRADED_LIO_DRIFT|WAITING_FOR_MAP",
  "motion_episode_id": 3,
  "wheel_episode_delta_xy_m": 0.03,
  "wheel_episode_delta_yaw_deg": 30.1,
  "pslam_episode_delta_xy_m": 0.82,
  "pslam_episode_delta_yaw_deg": 1.2,
  "last_good_map_stamp": 0.0,
  "last_rejected_reason": "rotation_translation_inconsistent"
}
```

---

## 11. 测试清单

### 11.1 离线单元测试

至少执行：

```bash
pytest -q \
  tests/test_go2w_experimental_backend.py \
  tests/test_local_scan.py \
  tests/test_autonomous_explorer.py \
  tests/test_live_candidate_goal_generator.py \
  tests/test_exploration_budget.py \
  tests/test_search_state_store_spatial.py \
  tests/test_go2w_live_ui_status.py \
  tests/test_plain_slam_webui_3d.py \
  tests/test_spatial_pose_validator.py \
  tests/test_regression_search_20260901_211756.py
```

再执行：

```bash
python3 -m py_compile \
  app/navigation/go2w_experimental_backend.py \
  app/live_robot/autonomous_explorer.py \
  scripts/go2w/run_semantic_exploration.py \
  scripts/go2w/plain_slam_web_bridge.py

bash -n scripts/go2w/start_autonomous_search_web.sh
bash -n scripts/go2w/start_plain_slam_mapping.sh
git diff --check
```

### 11.2 拓扑契约测试

构造同时含 `P1`、`F01`、`obj_001`、`obj_002` 的完整导航图，断言 WebUI投影结果：

```text
nodes = obj_001, obj_002
edges endpoints ⊆ {obj_001, obj_002}
P1 absent
F01 absent
OBSERVED_FROM absent
FRONTIER_TO absent
```

### 11.3 地图坐标测试

用合成房间点云生成：

1. 同位姿重复帧；
2. 原地旋转后的正确世界帧；
3. 原地旋转却带1米假平移的错误帧；
4. `pslam_odom` scan和 `pslam_map` map的frame mismatch；
5. reset前后的迟到帧；
6. 覆盖长走廊和多个房间、总点数超过50万的大范围合成地图。

断言：

- 正确世界帧不产生旋转副本；
- 错误运动段不能进入永久地图；
- 不同frame不能直接混合；
- 新 `map_3d` 可以替换旧优化历史；
- reset后旧session消息不能污染新地图；
- 大范围地图超过缓存上限后，起点、终点和中间区域都仍有代表点；
- `web_display_points` 不超过显示预算，但其边界与完整地图边界一致；
- 连续加入新区域不会按时间删除最早的房间；
- 未产生新map revision时不重复生成和发送同一份大点云。

---

## 12. 真机验收顺序

### 12.1 验收A：静止60秒

用户不操作遥控器，记录 IMU、Pandar、fused odom、pslam odom和地图。

通过条件：

```text
无 timestamp backward/duplicate
fused XY漂移 ≤ 0.05 m
pslam XY漂移 ≤ 0.10 m
地图边界不持续膨胀
同一墙面保持单层
mapping_health = HEALTHY
```

### 12.2 验收B：遥控器左右转

用户依次执行左30°、左30°、右30°，每次停5秒。

通过条件：

- LIO与fused yaw同号且误差不大于5°；
- 每次原地转向 pslam 假平移不大于0.20米；
- 地图不围绕机器人复制成三片或花瓣；
- 原有墙、桌、柱保持固定；
- 地图 bounds 不因纯旋转明显扩大；
- Place保持同一个。

### 12.3 验收C：遥控器360°闭环

用户低速原地转一周。

通过条件：

- 地图结构闭合，起始方向和结束方向为同一个场景；
- 无多份旋转副本；
- pslam位置闭环误差不大于0.30米；
- yaw闭环误差不大于10°；
- 健康状态全程不虚假恢复。

### 12.4 验收D：遥控器行走建图

用户先直行约1米，再转向，再继续直行；尽量经过新区域。

通过条件：

- 旧区域保持固定，不随机器人移动；
- 新区域在旧地图边缘连续出现；
- 地图范围只朝真实新区域扩大；
- 不把同一个房间按每次朝向重复绘制；
- `map_3d`更新时间随关键帧推进；
- WebUI刷新后地图不跳回局部scan；
- 行走到新区域后，起始区域仍保留在全局地图中；
- `/api/slam/map3d` 同时报告SLAM源点数、全局缓存体素数和Web显示点数；
- WebUI即使只显示约5万个采样点，也必须覆盖完整地图范围，不能只剩机器人附近区域；
- 典型浏览器上旋转、缩放地图保持可操作，不因盲目提高点数卡死页面。

### 12.5 验收E：WebUI逻辑60°

在清场、遥控急停可用和操作员监督条件下，执行一个右转60°逻辑目标。

通过条件：

```text
primitive = r30 + r(remaining)
observed total ≈ -60°
逻辑goal只有累计完成后才成功
地图仍固定
```

### 12.6 验收F：自主搜索

布置一个初始视角看不到、需要转向或短距离移动后才能看到的目标。

通过条件：

- 三次 local scan后能进入 frontier/route分支；
- 搜索不会三四步自动结束；
- 没找到且仍有开放区域时继续搜索；
- 找到目标后停稳、复核并结束；
- WebUI语义拓扑只有对象；
- 搜索过程中固定世界三维地图持续扩展；
- session没有 `candidate_generator_error`、`PERCEPTION_ERROR` 或未分类异常。

---

## 13. 部署步骤

1. 在主机完成代码和测试；
2. 只同步本计划涉及的文件到 `/home/unitree/robotscene`，保留远端定向备份；
3. 在主机重新构建 `go2w_plain_slam_bridge`；
4. 有序停止旧 WebUI、旧 mapping和旧 relay；
5. 确认 `/go2w/slam/web_scan` 只有一个 publisher；
6. 确认 `/go2w/odom/fused` 只有一个 publisher；
7. 启动 mapping，先通过静止健康门；
8. 启动 WebUI；
9. 先做遥控建图验收，再启用自主运动；
10. 打开：`http://192.168.123.18:8765/`。

启动后检查：

```bash
curl -s http://192.168.123.18:8765/api/status
curl -s http://192.168.123.18:8765/api/search/readiness
curl -s http://192.168.123.18:8765/api/slam/map3d
curl -s http://192.168.123.18:8765/api/search/semantic-map
```

不得在旧 LIO已经发散的进程上只清 Web voxel 后继续验收，必须新建 mapping session。

---

## 14. 禁止采用的假修复

- 禁止把 `max_turn_deg_per_action` 直接从30改成60来绕过分段和里程计闭环；
- 禁止在前端把“60°”文字改成“30°”掩盖实际动作；
- 禁止删除内部 Place/Frontier，正确做法是只改变 WebUI对象拓扑投影；
- 禁止把 candidate generator 异常继续吞成空候选；
- 禁止简单增加搜索步数来掩盖三四步结束；
- 禁止在 Three.js 中逆旋转错误点云；
- 禁止把 `pslam_odom` 点直接改 header 为 `pslam_map`；
- 禁止在错误 LIO位姿上依靠体素滤波“滤掉重影”；
- 禁止只把 `max_web_points=20000` 改大，而继续让4万体素缓存按时间删除旧地图；
- 禁止把Web显示采样点数当作SLAM完整地图点数；
- 禁止只在运动时冻结地图、停稳后继续接受已经漂移的位姿；
- 禁止用当前未确认外参宣称动态 SLAM验收通过；
- 禁止在地图未健康时放开自主平移搜索。

---

## 15. 最终交付物

执行 AI 完成后必须提交：

1. 修改文件清单及每个文件的作用；
2. 本次失败会话的回归测试；
3. 60°逻辑动作分段和累计里程计证据；
4. 对象拓扑截图或快照，证明无 `P/F` 节点；
5. logger异常修复和超过10步搜索证据；
6. 动态 LIO对齐分析报告；
7. 静止、30°、360°和直行的 bag及指标表；
8. 固定世界三维地图的遥控行走截图/录屏；
9. 大范围地图容量报告，包含SLAM源点数、缓存体素数、Web显示点数、地图范围和旧区域
   保留检查；
10. `/api/slam/map3d` 最终健康快照；
11. 真机部署路径、启动命令、进程和 ROS publisher唯一性检查；
12. 最终 WebUI 地址；
13. 尚未通过的项目必须明确列出，不得用 `HEALTHY` 或“已完成”掩盖。

只有第12节 A-F 全部通过，才能宣告本计划完成。
