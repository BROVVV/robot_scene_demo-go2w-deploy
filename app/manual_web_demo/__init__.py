"""Go2-W Manual WASD+QE Web Demo.

An independent, small web demo that shows the Go2-W built-in camera in a
browser, drives the existing ``/go2w/motion`` high-level Action chain through
WASD+QE short pulses, and asynchronously lists the main visible objects using
the project's already-configured SiliconFlow vision configuration.
"""

from __future__ import annotations

__version__ = "1.0.0"
